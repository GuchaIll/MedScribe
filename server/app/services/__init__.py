"""
Application services — orchestrate domain logic behind API routes.

Services are the bridge between HTTP routes and core domain objects.
They manage transactions, coordinate multiple repositories, and
encapsulate business rules that don't belong in a single model.
"""
