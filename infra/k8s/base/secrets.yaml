# Placeholder Secret. Do NOT commit real values — populate via your secret
# manager (Sealed Secrets, External Secrets, SOPS, or `kubectl create secret`).
#
#   kubectl -n medscribe create secret generic medscribe-secrets \
#     --from-literal=DATABASE_URL='postgres://...' \
#     --from-literal=JWT_SECRET='...' \
#     --from-literal=GROQ_API_KEY='...' \
#     --from-literal=SPEECH_REMOTE_API_KEY='...'
apiVersion: v1
kind: Secret
metadata:
  name: medscribe-secrets
  namespace: medscribe
type: Opaque
stringData:
  DATABASE_URL: "postgres://postgres:postgres@postgres:5432/medscribe?sslmode=disable"
  JWT_SECRET: "change-me"
  GROQ_API_KEY: ""
  SPEECH_REMOTE_API_KEY: ""
